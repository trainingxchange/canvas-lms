(function() {
  define(['ember'], function(Ember) {
    var LearningMasteryView;

    return LearningMasteryView = Ember.View.extend({
      templateName: 'learning_mastery'
    });
  });

}).call(this);
